{
  "description": "trackIT",
  "scripts": {
    "start": "cd api && node index.js",
    "dev": "nodemon index.js",
    "test": "jest --setupFiles ./tests/setup/config.js --watchAll --detectOpenHandles --runInBand --verbose",
    "unitTests": "jest --testPathPattern=/units/ --watchAll --verbose",
    "coverage": "jest --testPathPattern=/units/ --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.8",
    "express": "^4.17.1",
    "express-session": "^1.17.3",
    "jest-fetch-mock": "^3.0.3",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.8.0",
    "sql-template-strings": "^2.2.2"
  }
}
